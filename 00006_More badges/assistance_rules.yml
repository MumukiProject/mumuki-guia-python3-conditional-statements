---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    submission_passed_with_only_these_warnings:
    - "write_badge HasRedundantBooleanComparison"
  then: "don't forget that simplest boolean comparisons like `x == True` and `x ==
    False` can be simplified as just `x` and `not x`, respectively \U0001F60E"
