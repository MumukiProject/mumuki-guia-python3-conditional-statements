expectation "`envido_value` must not have a return for every possible card":
through `envido_value` ! (
    returns with 1 ||
    returns with 2 ||
    returns with 3 ||
    returns with 4 ||
    returns with 5 ||
    returns with 6 ||
    returns with 7
  );