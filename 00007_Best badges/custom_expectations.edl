expectation "`write_best_badge` must **invoke** `write_badge` passing all its required arguments": 
  (within `write_best_badge` calls `write_badge`) or (! uses `write_badge`);  

expectation "`write_best_badge` must **invoke** `write_badge` passing exactly 4 arguments": 
  (within `write_best_badge` calls `write_badge` with (somehing, somehing, somehing, somehing)) or (! uses `write_badge`);  

expectation "`write_best_badge` must **invoke** `write_badge, passing a boolean expression as its fourth argument`": 
  (within `write_best_badge` calls `write_badge` with (somehing, somehing, somehing, nonliteral)) or (! uses `write_badge`);  
