---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_expectations_failed:
    - "* Not:UsesIf"
  then: "`write_badge` already includes an `if` in its definition - now it's time
    to determine which boolean expression to pass as an argument."
