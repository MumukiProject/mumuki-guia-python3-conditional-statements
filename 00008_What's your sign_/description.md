💪 Let's practice! We need a `sign` function that, given a number, returns:

* `1` if the number is positive;
* `0` if the number is zero;
* `-1` if the number is negative.

> Define the function `sign`.
